import gsap from 'gsap'
import { useEffect, useRef } from 'react'
import './style.css'
import { useFrame } from '@react-three/fiber'
import { CameraControls, useGLTF, Texture, useTexture } from '@react-three/drei'



export default function Projects() {

    return (
        <>

    </>
    )
}

